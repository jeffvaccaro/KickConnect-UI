version: 1
applications:
  - appRoot: "."
    frontend:
      phases:
        preBuild:
          commands:
          - pwd
          - ls -alh
          - node -v
          - npm -v
          - npm install
        build:
          commands:
            - pwd
            - ls -alh
            - npm run build
            - ls -alh dist
            - ls -alh dist/kickConnect
      artifacts:
        baseDirectory: dist/kickConnect/browser
        files:
          - '**/*'
      cache:
        paths:
         - node_modules/**/*